name: ocr_bpjs
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  google_fonts: ^4.0.4
  cupertino_icons: ^1.0.2
  persistent_bottom_nav_bar: ^5.0.2
  mask_for_camera_view: ^2.0.4
  flutter_carousel_widget: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
